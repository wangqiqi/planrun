# 测试报告 · 章节模板

母版结构；项目可在 Contract 或 `learn/` 增删节，但 **§1 摘要 · §8 复现 · §10 结论** 建议保留（L1 锚点友好）。

## §0 元数据头

```markdown
# {产品名} · {报告定位}

> **读者**：…  
> **定位**：发版前全栈回归 benchmark  
> **版本**：**v{x.y}** · {date}  
> **Sprint**：`{sprint_id}`  
> **基线 commit**：`{short_sha}`  
> **环境**：{environment_hint · 无密钥}  
> **Blocker**：**无** | 列出 P0
```

## §1 执行摘要

| 层级 | 命令 | 结果 | 墙钟 |
|------|------|------|------|
| … | `…` | ✅ PASS / ❌ FAIL | ~Ns |

**结论**：一段话（是否可作发版 benchmark）。

可选：`详细日志（本地 · gitignore）：{log_dir}/{id}.log`

## §2 测试矩阵

### 2.1 自动化分层

| 层 | 覆盖 | 状态 |
|----|------|------|

### 2.2 E2E / 浏览器（若有）

| 套件 | 用例数 | 说明 |
|------|--------|------|

### 2.3 域 / 模块（若有）

列表或表 · 来自 pytest 域 / 项目 verify 分区

## §3 业务闭环（可选 · 项目填）

场景表：角色 · 路径 · 结果

## §4 性能 benchmark（可选）

> 验收口径：**{perf_verify_script} 全 section 绿**；下表为采样。

子表由 Contract `tier: perf` 套件解析填入。

## §5 交付走查（可选 · `delivery.include`）

| 维度 | 结论 | 备注 |
|------|------|------|

**Blocker** / **Decision needed**

## §6 本 Sprint 修复项

| 问题 | 处理 |
|------|------|

无缺陷时显式写「无 P0/P1」。

## §7 Skip / 环境说明

| 项 | 说明 |
|----|------|

## §8 复现命令（benchmark 套件）

 fenced `bash` 块：Contract 套件命令原样列出 · 先决条件一句

## §9 关联文档（可选）

| 文档 | 用途 |
|------|------|

## §10 结论

Done when 是否闭合 · 后续回归对照哪几节 · 差异阈值（如 perf >20% 复评）

---

大纲副本 → `.cursor/templates/test-report-outline.md`
