---
description: 执行规范 — 修 bug 流程（复现、根因、回归、CHANGELOG）
globs:
  - "**/*"
alwaysApply: false
---

# 执行规范 · Bug 修复

方法论入口：**debug** skill（系统循环 · 铁律「无根因不提修复」）。本 rule 管执行与反馈落点。

## 1. 复现与范围

- 确认症状、环境、触发步骤；无法复现时先补最小复现或测试（**禁止**无复现盲改）。
- 区分 **bug** vs **预期变更**；后者走规划而非 hotfix。

### Issue / Bug 报告（吸收自 awesome-cursorrules-zh）

开 Issue 或向他人描述 bug 时，至少包含：

| 字段 | 内容 |
|------|------|
| **环境** | OS · 运行时/浏览器版本 · 相关配置 |
| **复现步骤** | 编号步骤，他人可跟做 |
| **预期** | 正确行为 |
| **实际** | 观察到的错误（含报错/截图） |
| **范围** | 是否回归 · 首次出现版本（若已知） |

信息不足 → 先补再改代码（见 **debug** skill）。

## 2. 根因

- 定位根因后再改；避免 symptomatic patch。
- 引用文件/行号说明因果链；多组件先定位断裂边界（见 **debug**）。

## 3. 修复

- 最小 diff；优先加回归测试（若项目有测试文化）。
- 运行 task 验收或项目 verify；勿标记完成而未验证。

## 4. 反馈

- 用户可见修复写入 CHANGELOG `### Fixed`（见 `rules/feedback/changelog.mdc`）。
- 若暴露流程/约定缺口，运行 **`/learn`** 更新 `.cursorGrowth/learn/`（通用规则缺口 → 用户授权后补 `.cursor/rules/`）

## 5. 与 run

- plan 任务 ID 可用 `TASK-*`；hotfix 可走 **git** + CHANGELOG
- `/run` 失败自修 ≤2 轮仍失败 → plan 标 `⚠️` 回流 `/plan`（循环见 **debug**）

## 6. Follow-up 与重复症状

- 同症状 **≥2** 已发布 patch → **不得** 直接第三个 hotfix； **`/plan`** §Follow-up 或 **`SPIKE-*`**（`.cursor/templates/spike-regression-cluster.md`）
- 改 progress / 长任务 UI / 弹窗 z-index / 错误 message / 格式检测 → 读对应 rule：`async-progress` · `long-running-ui` · `modal-layering` · `error-context` · `single-detector`
