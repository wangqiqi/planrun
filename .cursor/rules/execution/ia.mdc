---
description: 信息架构（IA）普遍性原则 — 工作流正交、角色逐级放权、分支点；项目路由矩阵见 docs/design/ 或 learn/
globs:
  - plan.md
  - docs/design/**
  - docs/**/IA*.md
  - docs/**/*信息架构*
  - docs/**/*information-architecture*
  - "**/layouts/**"
  - "**/Sidebar*"
  - "**/AppLayout*"
  - "**/navigation*"
alwaysApply: false
---

# 执行规范 · 信息架构（IA）

> **UX · 结构层特化** — IA ⊂ UX（Garrett 结构层 + 部分骨架）。UX 总览与分流 → `rules/execution/ux.mdc` · **ux** skill。表现层 / i18n 走 **delivery**。

通用原则在本文。**项目专属**（路由表、角色矩阵、线框）→ `docs/design/<产品>-ia.md`（或团队约定路径）· `/learn` → `.cursorGrowth/learn/`。**不**把业务路由、角色名、菜单文案写进 `.cursor/` 母版。

完整清单与规划流程 → **ia** skill。

## 两维分离（必记）

| 维度 | 策略 | 反例 |
|------|------|------|
| **角色（RBAC / ABAC）** | 同一页面 · 不同**默认入口**与**操作可见性**；权限**逐级放开**，非每人一套独立 App | 为每个角色复制同一功能的整页 |
| **工作流** | **一页一主意图**；跨流只在**命名分支点**交接 | Dashboard 同时主叙事「监控 + 审批 + 履约 + 系统配置」 |

**架构分层词**（域 / 环 / 层 / 模块名）可保留在文档与 API；**不必**原样作为用户一级导航。

## 工作流正交 · R1–R4

| # | 规则 |
|---|------|
| **R1** | 每个路由/主屏有且仅有一个 `primaryWorkflow`（用户能一句话说出「我现在在做什么」） |
| **R2** | 次工作流只能**摘要 + 深链**；禁止与主 CTA **同级**的第二主按钮 |
| **R3** | 跨工作流协作走**分支点**（modal / drawer / 深链 / 来源 query）；不在 A 屏嵌入 B 工作流完整工具栏 |
| **R4** | 分支点 UI：**一句说明 + 单一主操作**；不在分支态混入无关工作流表单 |

## 角色交叉 · C1–C4

| # | 规则 |
|---|------|
| **C1** | **共享页面，不复制页面**（除非信息架构完全不同且无法 gated） |
| **C2** | 差异在**入口权重与写操作**，不在布局分叉 |
| **C3** | 高权限角色**继承可达路由**，但**默认着陆仍走其主责工作流最短路径** |
| **C4** | 只读与读写：**同一壳** + 隐藏/禁用/鉴权写操作 |

**兼岗 / 多角色**：按**当前任务或最近分支意图**选轨，不按角色标识切换整套 UI。

## 实体中心工作台（可复用模式）

适用于：**多步交付**、**案件/工单**、**项目制**、**订单履约**等后台。

- 顶栏 **当前实体**（项目 / 订单 / 案件 / 客户）可切换
- **线性 Tab 或阶段条**表达主流程（通常 3–7 步）
- 页内 **状态摘要 + 主 CTA** 与当前实体同屏
- 进入工作台后 **全局导航瘦身或折叠**，宽度让给作业区

**卫星工作流**（外部集成、监控队列、平台配置）从主工作台 **发射**；完成后沿 **来源上下文**（`from` query、面包屑、来源条）**回流**，而非与主流程平级占满侧栏。

## 适用边界（避免过度套用）

| 更适合本文 | 不必强行套用 |
|------------|--------------|
| 多角色 B 端后台 · 运营/审核/配置并存 | 纯营销站 · 单流程 SaaS 向导 |
| 有 RBAC 与 3+ 条并行工作流 | 无角色差异的 CRUD 管理页 |
| Dashboard + 深链路业务 | 移动端单任务 App |

## 与 plan / delivery 分工

| 场景 | 动作 |
|------|------|
| 新后台 / 导航大改 / 「迷路」反馈 | **ia** skill 或 `SPIKE-*`；产出 `docs/design/*-ia*` |
| Sprint Done when 含 IA / 导航 | **delivery** §5 导航与 IA + 本文 R1–R4 抽检 |
| 仅视觉密度 / 组件样式 | 项目设计规范 / token Sprint；**不**夹带 IA 重构 |

## 反模式（标 High）

- 人人同一首页，KPI 与多个工作流**并列主 CTA**
- 侧栏大量平级项，无角色/意图差异的**默认着陆**
- 在列表/队列 drawer 内嵌**另一工作流**的配置表单（如审核抽屉里的部署/计费设置）
- 用内部架构词汇作一级菜单，用户无法映射到「今天要完成的事」
