---
description: VIBE development — documentation, tests, API alignment
globs:
  - "**/*"
alwaysApply: false
---

# VIBE SOP

- **Document first**: spec or plan acceptance before large multi-file implementation.
- **Test early**: add/update tests with behavior changes; green before ✅ task.
- **API contract**: agree request/response shape before parallel frontend/backend work.
- **Align often**: grep field names and routes against docs when touching both sides.
- **Layer order** (B/S): UI → interaction → API surface → backend → persistence — skip only if user confirms.
- **Deps before NIH**: prefer permissive OSS → thin adapt / vendor → custom only with rationale (`oss-first.mdc`).
- New projects: prefer `/scaffold` + `/plan` over ad-hoc directory sprawl.
- **Pre-merge / pre-release product check**: UI/feature sprints → **`/delivery`** before **`/release`** (see **delivery** skill).

## 垂直切片（full-stack feature）

新能力跨持久化 + API + 客户端时，**默认按切片交付**（每层可单独 ✅ TASK）：

| 序 | 层 | 要点 |
|----|-----|------|
| 1 | **契约** | 请求/响应 · 错误码 · 分页（**api** skill；OpenAPI 若已有） |
| 2 | **持久化** | migration / schema / 存储（若需要） |
| 3 | **领域服务** | 业务逻辑，非 HTTP 入口 |
| 4 | **API 面** | routes · handlers · auth/校验 |
| 5 | **测试** | 单测 + 集成；配套 `verify_<feature>.sh` L1（`rules/feedback/verify.mdc`） |
| 6 | **客户端类型** | types / client wrapper 与契约同步 |
| 7 | **UI** | 页面/组件接线 |

**禁止**未定契约时前后端并行改字段名。跳层须用户确认。

拆 TASK → **plan** 阶段 2 引用本表 · 验收分层 → verify.mdc。

## 长任务 / 异步进度（横切）

跨 **Worker + API + 轮询 UI** 或 **多阶段上传/导入** 时，除垂直切片外须联读：`async-progress.mdc` · `long-running-ui.mdc` · `modal-layering.mdc` · `error-context.mdc`。

## 选型横切

新依赖、vendor、或大块自研 → `oss-first.mdc` · `submodule.mdc`（规划期可勾选 **plan**「依赖 / 自研选型」）。
