---
description: 测试文件 SOP — Vitest/Jest/Playwright/pytest；链 test skill
globs:
  - "**/*.test.*"
  - "**/*.spec.*"
  - "**/*.feature"
  - "**/tests/**"
  - "**/e2e/**"
  - "**/playwright/**"
alwaysApply: false
---

# 执行规范 · 测试文件

编辑测试路径时加载。流程细节见 **test** skill · `rules/feedback/verify.mdc`。

## 单元 / 集成

- 测行为与契约，非实现细节（DOM 选择器、私有函数名）
- 一断言一意图；表驱动优于复制粘贴 case
- Vitest/Jest：`describe` 按模块；mock 最小化，优先 fake 边界
- pytest：fixture 复用；`@pytest.mark` 标 slow/integration

## E2E（Playwright / Cypress）

- 关键用户路径优先；稳定 `data-testid` 或 role 选择器
- 本地：`npx playwright test` 或项目 script；`webServer` 或先起 dev
- 不测 flaky 时序；必要时 `expect.poll` / 显式 wait

## BDD / Gherkin（吸收自 awesome-cursorrules-zh）

编辑 `**/*.feature` 时加载。流程细节仍见 **test** skill。

- **用户视角**：Scenario 用业务语言，非实现细节（CSS 选择器、内部函数名）
- **一行为一场景**：独立、可重复；共享前置放 `Background`
- **Given/When/Then**：前置状态 · 动作 · 可观察结果；避免 And 链过长
- **框架**：Cucumber · Behave · SpecFlow · jest-cucumber — 与项目 lockfile 一致
- **与 E2E 分工**：`.feature` 描述契约；步骤定义（step defs）保持薄，复用页面对象/fixture

## 纪律

- **红 → 绿 → 重构**（见 **test** skill TDD 节）
- 新 bug → 先补回归再修实现
- 红测不 ✅ plan task

## 验收

- 开发 TASK：`./scripts/test.sh` 或 stack 默认
- Sprint/P0：`./scripts/verify.sh` 或 `runner.sh verify`
