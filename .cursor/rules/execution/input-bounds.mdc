---
description: 输入边界与安全默认 — clamp、白名单、敏感默认拒绝（通用）
globs:
  - "**/api/**"
  - "**/schemas/**"
  - "**/validators/**"
  - "**/dto/**"
  - "**/*Schema*"
  - "**/*Config*"
  - "**/*settings*"
alwaysApply: false
---

# 执行规范 · 输入边界与安全默认

**二级规范**：随 API/schema/配置路径加载。审查入口仍是 **api** · **security**，不新增 slash。

## 必须

1. **数值 clamp**  
   百分比、偏移、速率、分页 `limit`、超时等：定义 **下限与上限**；越界夹紧或 4xx，禁止静默用脏值。

2. **枚举 / 模式白名单**  
   冲突策略、排序、导出格式等：只接受已知字面量；未知值拒绝或回退到文档化默认。

3. **敏感能力默认拒绝（deny-by-default）**  
   插件/扩展、远程代码、宽 CORS、调试后门、自动发现入网等：未显式配置时 **关 / 拒**；官方或签名白名单可默认启用（须文档说明）。

4. **配置校验失败可回退**  
   端口、URL、路径等非法时：回退安全默认并 **可观测**（日志/指标），禁止半初始化状态对外服务。

5. **破坏性导入 / 覆盖**  
   见 **delivery**「导入与破坏性操作」：dry-run/预览 + 二次确认；写路径事务化（若存储支持）。

## 与安全 / API

- 清单勾选 → **api** skill「边界与默认」· **security**「输入与输出」
- 认证「默认 deny」与扩展宿主 → `extensibility.mdc`（若项目有插件面）

## 项目特化

具体上限数字、配置键名 → `.cursorGrowth/learn/`，**勿**写进本 rule。
