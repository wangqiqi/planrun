---
description: 开源优先选型 — 宽松授权依赖 → vendor 改编 → 最后自研（通用）
globs:
  - "**/package.json"
  - "**/Cargo.toml"
  - "**/go.mod"
  - "**/pyproject.toml"
  - "**/requirements*.txt"
  - "**/composer.json"
  - "**/Podfile*"
  - "**/build.gradle*"
  - "**/.gitmodules"
  - "**/vendor/**"
  - "**/third_party/**"
  - "**/3rd/**"
alwaysApply: false
---

# 执行规范 · 开源优先（oss-first）

**目标：尽快、稳定地交付产品能力。**  
选型默认顺序：**社区稳定的宽松开源** → **薄适配 / 仓内改编（vendor）** → **自研（须登记理由）**。

本 rule 为 **二级执行规范**（随依赖/vendor 路径 glob 加载）。用户入口仍是 **`/plan`** · **`/master` → deps** · **security**，**不**单独提供 slash skill。

## 决策树

```
有社区稳定、授权宽松的同类实现？
  ├─ 无 → 自研（在 plan / ADR 写清「无同类」）
  └─ 有 → 能否直接用包管理器依赖？
        ├─ 能 → 依赖接入 + 薄适配（胶水层）
        └─ 不能开箱即用（改源码 / 裁剪 / 钉版本）
              → 放入项目约定的 vendor 目录改编
                    │
                    ▼
              改编后仍不满足产品硬约束？
                ├─ 是 → 才允许自研（写清「为何开源不适应」）
                └─ 否 → 用改编后的开源，禁止无理由推倒重写
```

| 优先做 | 最后才做 |
|--------|----------|
| 包管理器依赖 · vendor 改编 · 薄适配 | **自研** |

## 授权口径（依赖与 vendor）

| 类别 | 可否依赖 / vendor | 说明 |
|------|-------------------|------|
| **MIT / BSD / ISC / Apache-2.0 / Unlicense** 等宽松许可 | ✅ 默认选用 | 允许修改适配 |
| **MPL-2.0** | ⚠️ 慎用 | 改动文件开源义务；外挂另论 |
| **LGPL** | ⚠️ 慎用 | 动态链接/子进程通常可接受；静态捆绑须合规审查 |
| **GPL / AGPL** 作库依赖或 vendor 进产物 | ❌ 禁止 | 传染风险（除非产品本身同许可且用户确认） |
| **专有 / Commons Clause** | ❌ | 默认不符合开源优先目标 |

传递依赖同样扫描：新增直接依赖前，确认树中无未接受的 GPL 族库。

## 「不适应」须具体

允许的理由示例（写成可验证约束）：无中心/离线/不出域、体积或性能硬上限、平台/合规强制、协议面与现有传输无法对接。

**禁止**仅写：「我们更熟」「重写更爽」「开源不好用」（无具体点）。

## 评审口令（答不上来 → 改选型）

1. 社区有没有同类**宽松授权**实现？  
2. 不能开箱即用时，能否 **改一改 / 放进 vendor** 就够用？  
3. 为什么必须自研？（无同类 / 不适应的具体点）  
4. 这条路径是否**更快**完成产品？

## 与其它规范

| 主题 | 落点 |
|------|------|
| vendor / submodule 目录与溯源文件 | `submodule.mdc` |
| 依赖漏洞审计 · 默认拒绝扩展 | **security** skill · `security-sdlc.mdc` |
| 规划期勾选 | **plan** 阶段 1 |
| 防无谓自研扩 scope | `scope.mdc` · `vibe.mdc` |

## 禁止写进本 rule

具体产品功能表、具体包名清单、某一仓库的债务队列 → 项目 `docs/` 或 `.cursorGrowth/learn/`。
