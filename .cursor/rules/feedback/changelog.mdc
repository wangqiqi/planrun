---
description: 反馈规范 — CHANGELOG 结构与 plan 分工
globs:
  - CHANGELOG.md
  - plan.md
  - archive/**/*
alwaysApply: false
---

# 反馈规范 · CHANGELOG

读 `.cursor/config/release.json` 获取 tag 格式与文件名。

## 版本节

- 标题：`## [x.y.z] - YYYY-MM-DD`（或 config 约定）
- **新版在上**（`release.json` → `order: newest_first`）
- 未发布工作积压在 `## [Unreleased]`；打版时折叠进版本节
- **tag-per-commit**：每 commit 直接写 `## [x.y.z]`，仅本条任务 1～3 条
- **禁止同版本双节**：同一 `[x.y.z]` 不得出现两个 `## [x.y.z]` 块；follow-up 用同一节下 `### Fixed` / `### Changed` 追加，或合并叙事后只保留一节
- **follow-up 标注**：连续 patch 修同一症状簇时，条目末尾注明根因 Sprint（如 `` `sprint-N-followup` ``）便于 **`/learn`** 审计
- **序自检（母版）**：`bash .cursor/verify-super-cursor.sh` 校验根 `CHANGELOG.md` 中 `## [x.y.z]` 严格新→旧；破例 → FAIL（`## [Unreleased]` 可置顶）。混合仓 layout 模式见 `rules/feedback/verify.mdc`。

## 内容

- 用户可见行为、API 变更、修复、breaking changes
- 有 issue/PR 则链接
- 长进度叙事放 `.cursorGrowth/plan.md` 或 `.cursorGrowth/archive/`，勿堆进 CHANGELOG

### Sprint 小节结构（可选 · 业务仓库）

用户可见 Sprint 交付时，版本节下可用统一小节（**写业务差异**，不写 SOP 步骤）：

```markdown
### 主题（SPRINT-ID）

- **API / 服务**：…
- **前端 / CLI**（若有）：…
- **迁移 / 配置**（若有）：…
- **验收**：`verify_<feature>.sh` 并入聚合脚本
- **未做（backlog）**：…
- **任务**：TASK-… …
```

母版 `.cursor/` 规则变更 → `### Changed` 一行摘要即可，细则在 rules/skills。

## 与 plan

- **`.cursorGrowth/plan.md`**：本地 Sprint 状态 · **gitignore** · 不 commit
- **CHANGELOG**：对外发布说明
- 归档用 `.cursorGrowth/archive/` 时间戳文件名；plan ROADMAP 留本地索引

## 打版前

- [ ] Unreleased 已审阅分组
- [ ] 版本顺序正确
- [ ] 行为变更已更新文档

CHANGELOG 供 **`/learn`** 输入；摘要仅 `.cursorGrowth/learn/`。
