---
description: plan/run 闸门、验收、growth 边界
alwaysApply: true
---

# 工作流

| 条件 | 动作 |
|------|------|
| `PLANNING:true` | 仅 **plan** |
| 无 `PLAN_APPROVED` | 仅 **plan** |
| `gate-check`≠OK | 禁止编码 |

CLI：`.cursor/bin/runner.sh` — `gate-check` · `task-verify` · `verify` · `plan-check` · `next-task`

- 每任务：`task-verify` 通过才 ✅/commit
- Sprint/打版前：`verify`
- 打版：`release.mode`（`patch-per-task` | `tag-per-commit`；见 `rules/feedback/release.mdc`）
- `AUTONOMOUS:true` → **一次 `/run` Sprint 连跑**（详 `plan/reference/autonomy-chain.md`）；仅决策打断 · `⚠️` → `/plan`

## 规模门禁（plan≥5）

预估或已列 **todolist / TASK > 5** → **禁止**直接编码；先 **`/plan`** 写入 `plan.md` 并获确认（见 **plan** skill「规模门禁」）。≤5 的清晰小改可直述执行。

## 递归分解与防漂移闸门

- `/plan` 必须先冻结 L0 Goal、Done when、Out of scope，再划分 L1 Theme 和后续 TASK；
- 同层 TASK 必须互斥且完备；`Target`、`Owns` 和主验收不得跨多个 Theme；
- `/run` 只执行当前 `ACTIVE` 分支；发现新 Theme 或横向扩张时标记 `⚠️` 并回 `/plan`，不得把它追加到当前 TASK；
- 实现步骤留在 TASK 内部，不为单文件、字段或命令单独创建任务；
- 仍需方案决策的内容使用 `SPIKE-*` / `Decision needed`，不得和实现 TASK 混合；
- 若单 Sprint 的任务树需要超过 5 个同层节点，先重查分区轴；无法保持上下文闭合时拆 Sprint/Theme，而不是继续细切。
- Sprint **Goal** 须为能力/模块交付；打 tag · merge · 专归档等 **出口动作** 不得单独立项 — 见 **plan** `reference/sprint-goal-gate.md` · **`/release`**。

## 任务 ID

`config/workflow.json` → `task_id`：

| 前缀 | 行为 |
|------|------|
| `TASK-*`（`prefixes_autonomous`） | 常规实现任务；`/run` 主路径 |
| `SPIKE-` `REV-` `DOC-`（`prefixes_skip`） | **`next-task` 自动选下一 ⬜ 时跳过**；不 bypass `gate-check` |
| `REV-*` | **review** skill · **review** agent（只读） |
| `SPIKE-*` | **spike** agent · **plan** 调研（只读） |

`plan.md` 须含 **`**执行顺序**`** 行（旧模板 `**Order**` 仍兼容），供 `next-task` 解析。根目录 `plan.md` 默认 **`.gitignore`**，不纳入版本库；模板见 `.cursor/templates/plan.md`。

## Growth

项目特化与产出统一 `.cursorGrowth/`（坐标见 `workflow.json`）· 禁止写入目标项目的 `.cursor/` · 细则 **learn** skill · 引用纪律 **cursor-standalone**

**`.cursor/` 只读（仅目标项目）**：见 `core.mdc`「安装后只读」— 母版仓库可自由演进。
