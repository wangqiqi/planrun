---
description: 【高级】可发布软件使用说明书 — 故事线 · 配图 regen · Manual Contract
---

加载 skill **user-manual**：

1. 读 `config/manual.yaml` 或 `learn/user-manual.md`（Manual Contract）
2. 按 **五段流水线**：提取 → verify 门禁 → capture regen → L1 + Reader Test
3. **不是** delivery 走查 · **不是** test 行为断言

大改版 → **plan** `DOC-*`。发版前 regen → 见 `reference/regen-gates.md`。
