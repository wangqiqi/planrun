# Stack · Test Report

> **读者**：engineering · qa  
> **定位**：Sprint / 发版前回归 benchmark  
> **版本**：**v0.1** · _pending first regen_  
> **基线 commit**：_pending_  
> **环境**：local dev · no secrets  
> **Blocker**：**待首次采集**

---

## 1 执行摘要

| 层级 | 命令 | 结果 | 墙钟 |
|------|------|------|------|
| L1 gate | `bash scripts/verify.sh --l1` | _pending_ | — |
| Unit tests | `bash scripts/test.sh` | _pending_ | — |

**结论**：_Run `bash scripts/docs/collect_test_report.sh` then `/report`._

详细日志（本地 · gitignore）：`data/tmp/test-report/*.log`

## 8 复现命令

```bash
bash scripts/docs/collect_test_report.sh
bash scripts/verify/docs/verify_test_report.sh
```

## 10 结论

_Done when 闭合后填写 · 见 **test-report** `/report` skill._
