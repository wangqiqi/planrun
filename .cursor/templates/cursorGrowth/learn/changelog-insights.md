# Changelog insights（项目特化）

> 由 **`/learn`** 从 `CHANGELOG.md` 增量摘要。对外行为变更的**项目视角**解读 + **重复工作模式**（防回归）。

## 当前版本

| 项 | 值 |
|---|---|
| 最新 tag | （待填） |
| 闭合 Sprint | （待填） |

## 近期要点

（`/learn` 每次发版或 Sprint 后更新）

## 重复工作模式（可选 · `/learn` 全量审计时填）

> 定义：同症状/域多版本反复出现。通用防重复规则见 `.cursor/rules/execution/async-progress.mdc` 等；**本节只写本项目符号与版本号**。

| 症状簇 | 出现版本 | 根因类型 | 状态 |
|--------|----------|----------|------|
| | | | open / closed |

## 建议下一 Sprint

（来自重复模式 + plan 候选，待用户确认）
