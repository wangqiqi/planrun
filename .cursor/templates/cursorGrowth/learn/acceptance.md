# 交付验收 · 项目特化（/learn 可选创建）

> 供 **delivery** skill 读取。路径/design token/i18n/OpenAPI 因项目而异，填本文件即可。

## Design tokens / 视觉

| 项 | 本项目 |
|----|--------|
| Token 文件 | （例：`src/styles/tokens.css`） |
| 组件库 | （例：Ant Design / shadcn） |
| 禁止硬编码 | （团队约定） |

## i18n

| 项 | 本项目 |
|----|--------|
| 库 / API | （例：`vue-i18n` · `$t()` · `useTranslation`） |
| locale 目录 | （例：`src/locales/`） |
| 默认 locale | |
| 本版是否交付多语言 | 是 / 否（否 → delivery §2 走查须声明跳过理由） |

## API / 后端对接

| 项 | 本项目 |
|----|--------|
| OpenAPI / 接口文档 | |
| Mock / fixture | |
| DB schema / migration 文档 | |

## 浏览器走查（delivery §10）

| 项 | 本项目 |
|----|--------|
| 本版是否做浏览器走查 | 是 / 否（否 → 报告写 §10 skipped 理由） |
| 默认 URL（本地或预览） | （例：`http://127.0.0.1:3000`） |
| 关键页面路径（可选） | |

## 其他门禁

- （团队额外检查项，可链到 `rules/local/*.mdc`）

## 上次更新

- 日期：
- 来源：/learn · 手动
